namespace Combat.Core
{
    public enum CombatState
    {
        Idle,
        Moving,
        Attacking,
        Blocking,
        Stepping,
        Recovering,
        Staggered,
        Dead
    }
}